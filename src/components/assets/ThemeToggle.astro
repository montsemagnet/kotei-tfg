---
// Logo Component This component renders an SVG logo and accepts props for customization.
// Props: - class: A string of additional classes to style the SVG.
// - ...rest: Any other valid attributes for an <svg> element (e.g., id, style, role, aria-label, etc.).
// Example Usage: <Logo class="my-custom-class" /> or <Logo id="logo-id" style="width: 50px; height: 50px;" />
const { class: className = "", ...rest } = Astro.props;
import Toggle from "@/components/fundations/icons/Toggle.astro";
---

<button
  class={className}
  {...rest}
  role="switch"
  aria-checked="false"
  aria-label="Activa el mode fosc"
  data-theme-toggle
>
  <span class="sr-only" data-theme-toggle-label>Activa el mode fosc</span>
  <Toggle size="base" class="mx-auto" />
</button>

<script>
  function updateTheme(isDark) {
    document.documentElement.classList.toggle("dark", isDark);
    localStorage.setItem("theme", isDark ? "dark" : "light");

    const label = isDark ? "Activa el mode clar" : "Activa el mode fosc";
    // Update all theme toggles
    document.querySelectorAll("[data-theme-toggle]").forEach((button) => {
      button.setAttribute("aria-checked", isDark ? "true" : "false");
      button.setAttribute("aria-label", label);
      const sr = button.querySelector("[data-theme-toggle-label]");
      if (sr) sr.textContent = label;
    });
  }

  function initializeTheme() {
    const savedTheme = localStorage.getItem("theme");
    const systemPrefersDark = window.matchMedia(
      "(prefers-color-scheme: dark)"
    ).matches;
    const isDark = savedTheme ? savedTheme === "dark" : systemPrefersDark;
    updateTheme(isDark);
  }

  initializeTheme();

  // Attach event listener to all theme toggles
  document.querySelectorAll("[data-theme-toggle]").forEach((button) => {
    button.addEventListener("click", () => {
      const isDark = document.documentElement.classList.contains("dark");
      updateTheme(!isDark);
    });
  });
</script>
